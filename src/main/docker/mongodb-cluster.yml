version: '2'
services:
  rjmd-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/rjmd-mongodb-config --bind_ip 0.0.0.0
  rjmd-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  rjmd-mongodb-config:
    image: mongo:4.2.7
    container_name: rjmd-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
